name = "fofa-mcp"
main = "src/index.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

# 可选配置（通过 wrangler secret put 或 Cloudflare Dashboard 设置）：
# SERVER_URL  — 自定义域名，不设置则自动从请求推断
# JWE_SECRET  — 32 字节 base64，不设置则自动生成（冷启动后旧 token 失效）
# JWS_SECRET  — 32 字节 base64，同上
